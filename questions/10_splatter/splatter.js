//write the splatter function

const splatter = (...args) => {
  const finalVal = 0
  console.log(s[...args])
  argsArr.forEach(obj => finalVal+= Object.values(obj)[0])
  return finalVal//write code here
};

module.exports = { splatter };
